
const arr = [1,2,3];

console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());
console.log(arr.shift());

// 앞에서부터 추출 
// 추출한 요소가 없으면 undefined
// pop 은 뒤에서, shift 는 앞에서